{
  "name": "fit_stage_io",
  "version": "1.0.0",
  "description": "A web application created for online bodybuilding and fitness competitions.",
  "main": "index.js",
  "scripts": {
    "start": "node server/server.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "concurrently \"cd server && npm run watch\" \"cd client && npm run",
    "build": "cd client && npm run build",
    "format": "prettier --write 'client/src/**/*.{js,jsx,ts,tsx}"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^7.0.0"
  },
  "dependencies": {
    "bcrypt": "^5.1.1",
    "dotenv": "^16.4.5",
    "express": "^4.18.2",
    "nodemon": "^3.0.3",
    "prettier": "^3.3.3",
    "react": "^18.2.0",
    "sequelize": "^6.36.0",
    "vite": "^5.4.8"
  }
}
